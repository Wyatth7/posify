import React from 'react';
import styled from 'styled-components';

const StripeElementPaymentWrapper = styled.div``;

const StripeElementPaymentWrapper = (props) => {
 
 return();
};

export default StripeElementPaymentWrapper;